[package]
name = "rust-api"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.8"
tokio = { version = "1", features = ["macros", "rt-multi-thread", "net"] }
prometheus = "0.14"
once_cell = "1"
serde = { version = "1", features = ["derive"] }